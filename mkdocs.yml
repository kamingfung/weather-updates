site_name: Weather Updates
site_description: Public weather outlook summaries for farm regions.
site_url: https://weather-updates.kamingfung.workers.dev/
theme:
  name: material
use_directory_urls: true
# Nav is omitted — the custom index.html provides its own sidebar and iframe layout.
# MkDocs will still copy all files under docs/ (including archive/**) to site/.
